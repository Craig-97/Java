package w4x1;


/*
 * Abstract belief behaviour class, its only role is to partition home and belief behaviour
 * into separate types so they aren't used interchangeably.
 */

public abstract class BeliefBehaviour extends Behaviour {}
